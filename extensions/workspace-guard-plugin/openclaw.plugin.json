{
  "id": "workspace-guard-plugin",
  "name": "Workspace Guard Plugin",
  "description": "Restricts writes/deletes to the workspace and keeps configured paths read-only.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "readonlyPaths": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Workspace read-only paths. Relative paths resolve from the current workspace; absolute paths stay absolute."
      }
    }
  }
}
